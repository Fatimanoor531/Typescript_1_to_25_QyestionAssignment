"use strict";
let members = ["noori", "hoor", "haadi", "zulqarnain", "umer"];
let message = "who will play tomorrow?: ";
for (let i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
